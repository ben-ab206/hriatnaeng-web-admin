"use client";

import BookForm from "../BookForm";

const BooksNew = () => {
  return (
    <div>
      <BookForm isSubmitting={false} onSubmit={() => {}} />
    </div>
  );
};

export default BooksNew;
